<nav-bar ></nav-bar>
<ion-content>
  <ion-grid class="page-title">
    <ion-row>
      <ion-col col-2 (tap)="goBack()">
        <ion-icon name="md-arrow-back" class="back-button-custom" ></ion-icon>
      </ion-col>
      <ion-col col-8>
        <h1>{{title}}</h1>
      </ion-col>
    </ion-row>
  </ion-grid>
  <div class="test">
	  <div class="container">
		<img src="assets/img/admin_icon_hus.png" />
		<label>Restaurantlokation</label>
		<p>{{currentLocation.name}}</p>
	  </div>
	  <div class="container">
		<img src="assets/img/admin_icon_adresse.png" />
		<label>Adresse</label>
		<p>{{currentLocation.address.trim()}},<br>{{currentLocation.zip}} {{currentLocation.city}}</p>
	  </div>
	  <div class="container">
		<img src="assets/img/admin_icon_produkt.png" />
		<label>Min. antal produkter</label>
		<p>{{currentLocation.minProducts}}</p>
	  </div>
	  <div class="container">
		<img src="assets/img/admin_icon_chef.png" />
		<label>Køkkenchef</label>
		<p>{{chef.chefName}}</p>
	  </div>
	  <div class="container" *ngFor="let sub of currentLocation.subList; let i = index">
		<img src="assets/img/kurvDark.png" />
		<label>Afhændningssted {{i + 1}}</label>
		<p>{{currentLocation.subList[i].location}}</p>
	  </div>
  </div>
  <div id="buttonContainer">
    <button ion-button block color="#746f6f" id="seeSortiment" (tap)="openPage('LocationSortimentPage',{id: currentLocation._id})">
      Se sortiment
    </button>
    <button ion-button block color="green" id="editButton" (tap)="openPage('EditLocationPage', {id: currentLocation._id})">
      Rediger
    </button>
  </div>
</ion-content>
