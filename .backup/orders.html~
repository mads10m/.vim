<nav-bar></nav-bar>
<ion-content>
  <ion-grid class="page-title">
    <ion-row>
      <ion-col>
        <h1>{{title}}</h1>
      </ion-col>
    </ion-row>
  </ion-grid>
  <ion-content>
    <ion-refresher (ionRefresh)="verifyAll($event)">
      <ion-refresher-content
        pullingText="Træk ned for at opdatere"
        refreshingText="Opdaterer...">
      </ion-refresher-content>
    </ion-refresher>
    <div *ngIf="this.orders.paidOrders.length===1; else ordersTable">
      <hr>
      <p class="info-text">Du har ikke nogen bestillinger. Gå til sortiment for at finde produkter eller kurv for at gennemføre en bestilling</p>
    </div>
    <ng-template #ordersTable>
      <table id="ordersTable">
        <tbody>
        <tr>
          <th>Produkt</th>
          <th>Antal</th>
          <th>Pris</th>
        </tr>
        <tr class="order" *ngFor="let product of orders.paidOrders">
          <td class="productName">
            {{product[0].name}}
          </td>
          <td class="productAmount">
            {{product[1]}} {{product[0].isDaily?'portion':'stk.'}}{{(product[0].isDaily&&product[1]>1)?'er':''}}
          </td>
          <td class="productPrice">
            {{product[0].price*product[1]}} kr.
          </td>
        </tr>
        </tbody>
      </table>
      <div id="bottomDiv">
          <h1 id="totalCostText">Total</h1>
          <h1 id="totalCostPrice">{{this.orders.totalCost}} kr.</h1>
      </div>
	  <!--<div id="pickUpBar">
        <p>Afhentes {{todayString}} efter kl. {{pickupStart}}</p>
	  </div>-->
    </ng-template>
    <p class="info-text" style="color: green;">Træk ned for at opdatere</p>
  </ion-content>
</ion-content>
