#!/bin/bash

tmux ls
if [[ $(tmux ls | grep "main") =~ \(attached\)$ ]]; then                                                                                                                                         
	echo "true"
else
	echo "false"
fi
